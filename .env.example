# BSTT Compliance Dashboard - Environment Variables
# Copy this file to .env (development) or .env.production (production)
# and update values for your environment.
#
# IMPORTANT: Never commit .env or .env.production to version control!

# =============================================================================
# Django Backend Settings
# =============================================================================

# SECURITY WARNING: Generate a new secret key for production!
# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-change-in-production

# Debug mode - MUST be False in production
DEBUG=False

# Allowed hosts (comma-separated)
# Development: localhost,127.0.0.1
# Production: your-domain.com,www.your-domain.com
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS allowed origins (comma-separated)
# Development: http://localhost,http://localhost:3000
# Production: https://your-domain.com
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:3000,http://localhost:80

# Database path (default: SQLite in /app/data/)
DATABASE_PATH=/app/data/db.sqlite3

# =============================================================================
# React Frontend Settings
# =============================================================================

# API URL for the frontend to connect to backend
# In Docker with nginx reverse proxy, use /api (relative URL)
REACT_APP_API_URL=/api

# =============================================================================
# Docker Settings
# =============================================================================

# Container name prefix
COMPOSE_PROJECT_NAME=bstt

# =============================================================================
# Production Settings (only needed for docker-compose.prod.yml)
# =============================================================================

# Your production domain (used in nginx config)
# Example: bstt.xlcservices.com
# DOMAIN=your-domain.com

# SSL Certificate paths (relative to nginx/ssl/)
# SSL_CERT=fullchain.pem
# SSL_KEY=privkey.pem
